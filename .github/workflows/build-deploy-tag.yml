name: Build and deploy

on:
  push:
    tags: [ 'v*.*' ]

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: .github/workflows/docker-publish.yml
      with:
        TYPE: ${{ github.job }}

  server:
    runs-on: ubuntu-latest
    steps:
    - uses: .github/workflows/docker-publish.yml
      with:
        TYPE: ${{ github.job }}

  deploy:
    runs-on: ubuntu-latest
    needs: [frontend, server]
    steps:
    - uses: .github/workflows/oci-deploy.yml