name: Build and deploy

on:
  push:
    tags: [ 'v*.*' ]

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: docker-publish.yml@develop
      with:
        TYPE: ${{ github.job }}

  server:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: docker-publish.yml@develop
      with:
        TYPE: ${{ github.job }}

  deploy:
    runs-on: ubuntu-latest
    needs: [frontend, server]
    steps:
    - uses: actions/checkout@v2
    - uses: oci-deploy.yml@develop